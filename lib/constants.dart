import 'package:flutter/material.dart';

//Naming of constants starts with 'k', makes it simpler while typing just type k and you see suggestions for all constans
const kBottomContainerHeight = 80.0;
const kBottomContainerColor = Color(0xFFEB1555); //type is inferred with const
const Color kActiveCardColor = Color(0xFF1D1E33);
const Color kInactiveCardColor = Color(0xFF0A0E21);

const TextStyle kLabelTextStyle = TextStyle(
  fontSize: 18.0,
  color: Color(0xFF8D8E98),
);

const TextStyle kNumberTextStyle = TextStyle(
  fontSize: 50.0,
  fontWeight: FontWeight.w900,
);
